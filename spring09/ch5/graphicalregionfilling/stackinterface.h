#ifndef _fill
#define _fill

#include <stdio.h>
#include <stdlib.h>
#include "globals.h"

typedef struct {
  int x, y;
} point ;

status push_xy(p_S, x, y);
status pop_xy(p_S, (char *) pt) == ERROR ) ;

#endif
