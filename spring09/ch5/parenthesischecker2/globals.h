/***********************/
/* globals.h           */
/***********************/

#ifndef _globals
#define _globals

typedef enum { OK, ERROR } status;
typedef enum { FALSE=0, TRUE=1 } bool;
typedef void *generic_ptr;

#endif
