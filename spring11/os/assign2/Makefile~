# Programmer Eric Lima
# Makefile

ALL: grep1 grep2 sort

grep1: maingrep1.c
	gcc -o grep1 maingrep1.c

grep2: maingrep2.c
	gcc -o grep2 maingrep2.c

sort: mainsort.c
	gcc -o sort mainsort.c

clean:
	rm grep1
	rm grep2
	rm sort
	rm *~ 

